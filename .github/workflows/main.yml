name: secret-test
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        run: python streamlit-example.py
        env: 
          API_KEY: ${{ secrets.API_KEY }}
